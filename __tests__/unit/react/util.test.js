describe("deeCopy", () => {
  it("test sample", () => {
    expect(1).toBe(1);
  });

  it("test sample2", () => {
    expect(1).toBe(1);
  });
});

describe("getTreeSVG", () => {
  it("test sample", () => {
    expect(1).toBe(1);
  });

  it("test sample2", () => {
    expect(1).toBe(1);
  });
});
